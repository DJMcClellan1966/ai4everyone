# SICP Lab

**Structure and Interpretation of Computer Programs** (Abelson, Sussman, Sussman) — functional pipeline, streams, data abstraction, symbolic computation.

Uses **sicp_methods.py** at repo root (FunctionalMLPipeline, Stream, DataAbstraction, SymbolicComputation).

## Run (from repo root)

```bash
python learning_apps/sicp_lab/app.py
```

Open **http://127.0.0.1:5007**

## Contents

- **Functional ML Pipeline**: map, filter, reduce, compose, pipe, fold
- **Streams**: lazy sequences, integers, range_stream, map/filter on streams
- **Data Abstraction**: pairs (cons/car/cdr), trees
- **Symbolic Computation**: expressions, evaluate

Learn by book or level. Run demos for map, compose, stream, pair, symbolic.
