# Sandbox 05: Linguistics-Driven Augmentation

**Idea:** Use syntactic parser to get structure; generate variation that preserves parse (same POS pattern).

**Viability test:** Run SimpleSyntacticParser on 2–3 sentences; extract phrases; produce one "augmented" variant (e.g. same structure, placeholder).
